# ISPConfig HERITAGE 1.0.28

HERITAGE 1.0.28 recognizes ISPConfig's real outer page-form structure and
removes form metadata when navigation returns to a list. Twenty representative
forms are protected across 240 responsive light and dark runtime states.

The release is presentation-only and does not change ISPConfig controllers,
permissions, stored values or managed server operations.
