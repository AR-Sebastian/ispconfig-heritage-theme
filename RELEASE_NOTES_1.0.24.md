# ISPConfig HERITAGE 1.0.24

HERITAGE 1.0.24 gives populated tables a bounded horizontal scroll context,
keeps sticky row actions visible and prevents dormant empty rows from enabling
overflow geometry. Tablet records retain the adaptive HERITAGE card layout.

The release is presentation-only and does not change ISPConfig controllers,
permissions, stored values or managed server operations.
